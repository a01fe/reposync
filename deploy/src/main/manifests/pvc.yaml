apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: repos
spec:
  storageClassName: default
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
