pluginManagement {
  repositories {
    maven {
      url "${easNexusUrl}/maven-public"
    }
    gradlePluginPortal()
  }
}

include 'app'
include 'deploy'

includeBuild 'reposync-application-plugin'

rootProject.name = 'reposync'
